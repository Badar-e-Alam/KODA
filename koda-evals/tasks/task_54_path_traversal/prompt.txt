The `serve_file(base_dir, filename)` function in `server.py` is vulnerable to path traversal: `filename="../../etc/passwd"` escapes `base_dir`.

Fix it to reject any path that escapes `base_dir`. All tests in `test_server.py` must pass.
