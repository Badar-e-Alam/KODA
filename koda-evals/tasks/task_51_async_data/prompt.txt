The `loader.js` module fetches data but doesn't handle the case where the response is not JSON, causing an unhandled rejection.

Add proper error handling: if JSON parsing fails, return `{error: "Invalid JSON"}`. All tests in `loader.test.mjs` must pass.
